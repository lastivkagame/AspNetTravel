@*@{
        ViewBag.Title = "Index";
    }

    <h2>Index</h2>*@

@model  List<TravelAgenncyUI.Models.TourViewModel>
@{
    Layout = "~/Views/Shared/_LayoutPage.cshtml";
}

<div class="dashboard-wrapper">
    <div class="container pt-4" id="toursContainer">
            @{ Html.RenderPartial("ToursPartial", Model); }
        @*<div class="row">
            @foreach (var item in Model)
            {
                <div class="col-12 col-sm-6 col-md-4 col-lg-3 travel-card">
                    <div class="card">
                        <img class="card-img-top img-fluid p-2" src="@item.Image" alt="Card image cap">
                        <div class="card-body">
                            <h3 class="card-title">@item.Name</h3>
                            <p class="card-text">@item.Description</p>
                            <p class="card-text">Location: <strong>@item.Location</strong></p>
                            <p class="card-text">Flight: <strong>@item.Flight</strong></p>
                            <p class="card-text">@item.Price</p>
                            
                            @Html.ActionLink("Edit", "Edit", new { id = item.Id })
                        </div>
                    </div>
                </div>
            }
        </div>*@
        @Html.ActionLink("Get Locations", "GetLocations", null, new { @class = "btn btn-primary" })
    </div>
</div>
